    import { CapacitorConfig } from '@capacitor/cli';

    const config: CapacitorConfig = {
      appId: 'com.barkodx.app',
      appName: 'barkodx',
      webDir: 'dist',
      bundledWebRuntime: false
    };

    export default config;
    